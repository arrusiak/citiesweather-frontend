export const SET_WEATHERS = (state, weathers) => {
  state.weathers = weathers;
}

export const SET_WEATHER = (state, weather) => {
  state.weather = weather;
}

export const SET_PLACES = (state, places) => {
  state.places = places;
}