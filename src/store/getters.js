export const getWeathers = (getters) => {
  return  getters.weathers
}

export const getWeather = (getters) => {
  return  getters.weather
}

export const getPlaces = (getters) => {
  return  getters.places
}